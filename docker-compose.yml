services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gcba-acreditacion-app
    restart: unless-stopped
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgresql://postgres:88cbb2c0-beef-470a-b6b6-639c8f27da62@vpn.helio3.co:5433/acreditapp?schema=public&sslmode=disable
      API_PORT: 3000
      JWT_ACCESS_SECRET: dev_access_secret_change_me
      JWT_REFRESH_SECRET: dev_refresh_secret_change_me
      ACCESS_TOKEN_TTL_MINUTES: 15
      REFRESH_TOKEN_TTL_DAYS: 7
      CORS_ORIGIN: http://localhost:3000
      COOKIE_SECURE: "false"
    ports:
      - "3000:3000"

  postgres:
    image: postgres:16-alpine
    container_name: gcba-acreditacion-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gcba_acreditacion
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
